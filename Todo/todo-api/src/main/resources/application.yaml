spring:
  application:
    name: todo-api

  datasource:
    url : jdbc:postgresql://localhost:5432/TodoDb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto : update
    show-sql : true


server:
  port: 5000